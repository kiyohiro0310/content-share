server {
    listen 80;
    server_name sound-link.nk-1310.online;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://server:3000/; # Added trailing slash for cleaner routing
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Keep the 443 block fully commented out for now!

# server {
#     listen 443 ssl;
#     server_name sound-link.nk-1310.online;

#     # These paths are created after running the certbot command in Step 3
#     ssl_certificate /etc/letsencrypt/live/sound-link.nk-1310.online/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/sound-link.nk-1310.online/privkey.pem;

#     location / {
#         root /usr/share/nginx/html;
#         index index.html index.htm;
#         try_files $uri $uri/ /index.html;
#     }

#     location /api/ {
#         proxy_pass http://server:3000; # Matches container_name
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }